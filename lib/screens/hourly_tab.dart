import 'package:flutter/material.dart';

class HourlyTab extends StatelessWidget {
  const HourlyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hourly Data View'),
    );
  }
}
